<%= form_with(model: [@notice, Comentario.new], url: notice_comentarios_path(@notice)) do |form| %>
  <% if comentario.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comentario.errors.count, "error") %> prohibited this comentario from being saved:</h2>

      <ul>
        <% comentario.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :contenido, style: "display: block" %>
    <%= form.text_field :contenido %>
  </div>

  <%= form.hidden_field :user_id %>

  <%= form.hidden_field :notice_id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
